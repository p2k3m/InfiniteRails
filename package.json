{
  "name": "infiniterails",
  "version": "1.0.0",
  "description": "Infinite Dimension is a browser-based voxel survival-puzzle prototype built entirely with vanilla HTML, CSS, and JavaScript. Gather resources, craft ordered recipes, stabilise portals, and brave bespoke rulesets in every dimension as you race to recover the Eternal Ingot. This iteration introduces a responsive profile hub, optional Google SSO, geolocation capture, and a DynamoDB-ready scoreboard that showcases every explorer’s run.",
  "main": "script.js",
  "scripts": {
    "pretest": "node scripts/check-simple-experience.js && node scripts/check-inline-docs.js",
    "test": "vitest run",
    "test:e2e": "node tests/e2e-check.js",
    "check:manifest": "node scripts/check-manifest.js",
    "check:assets": "node scripts/validate-asset-manifest.js",
    "audit:assets:commits": "node scripts/per-commit-asset-audit.js",
    "sync:asset-digests": "node scripts/update-asset-digests.js",
    "build:vendor:three": "node scripts/build-three.js",
    "test:unit": "vitest run",
    "test:pre-release": "node scripts/run-pre-release-checks.js",
    "rollback:known-good": "node scripts/rollback-known-good.js",
    "prepublishOnly": "npm run test:pre-release",
    "preversion": "npm run test:pre-release"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.25.10",
    "playwright": "^1.55.0",
    "vitest": "^3.2.4",
    "yaml": "^2.8.1"
  },
  "dependencies": {
    "three": "0.161.0"
  }
}
