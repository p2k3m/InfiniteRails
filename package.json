{
  "name": "infiniterails",
  "version": "1.0.0",
  "description": "Infinite Dimension is a browser-based voxel survival-puzzle prototype built entirely with vanilla HTML, CSS, and JavaScript. Gather resources, craft ordered recipes, stabilise portals, and brave bespoke rulesets in every dimension as you race to recover the Eternal Ingot. This iteration introduces a responsive profile hub, optional Google SSO, geolocation capture, and a DynamoDB-ready scoreboard that showcases every explorer’s run.",
  "main": "script.js",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "node tests/e2e-check.js",
    "check:manifest": "node scripts/check-manifest.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.25.10",
    "playwright": "^1.55.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "three": "0.161.0"
  }
}
